<!DOCTYPE html>
<html>
  <head>
    <title> Tic Tac Toe </title>
    <link rel="stylesheet" type="text/css" href="/styles/styles.css">
  </head>
  <h1> Tic Tac Toe </h1>
  <body>
    <p> <%= "Click on a square to make a move" %> </p>
    <% board.cells.each_with_index do |cell, index| %>
      <% if board.mark_at(index) == "-" %>
        <form action="" method="post">
          <button name=player_move value=<%= index + 1 %> type='submit'><%= cell %>
        </button>
        </form>
      <% else %>
        <div class="cell">
          <p> <%= cell %> </p>
        </div>
        <% if (index + 1) % 3 == 0 %>
          </br>
        <% end %>
      <% end %>
    <% end %>
  </body>
</html>


